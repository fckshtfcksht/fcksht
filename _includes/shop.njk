<!DOCTYPE html>
<html>
<head><title>{{ title }}</title></head>
<body>
  <h1>{{ title }}</h1>
  {% if image %}<img src="{{ image }}">{% endif %}
  <p>{{ price }}</p>
  <a href="/">← Home</a>
</body>
</html>